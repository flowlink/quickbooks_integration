version: '3'
services:
  web:
    container_name: quickbooks-integration-container
    network_mode: bridge
    build: .
    env_file:
      - ./prod.env
    restart: always
    volumes:
      - .:/app
    logging:
      driver: gcplogs
